# mvp-starter


```sh
#clone repo 
git clone https://github.com/dhpatel15/mvp-starter.git

cd mvp-starter

# download dependencies
npm install

# set up MySQL schema and populate database
npm run seed

# start server on localhost
npm run server-dev

# run webpack to build client bundle
npm run react-dev
```